import { DefaultTheme } from 'styled-components';

export const light = {
  colors: {
    primary: "#efeff2",
    secondary: "#121214"
  }
}

export const dark: DefaultTheme ={
  colors: {
    primary: "#121214",
    secondary: "#efeff2"
  }
}